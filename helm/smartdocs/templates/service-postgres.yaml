apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartdocs.fullname" . }}-postgres
  labels:
    {{- include "smartdocs.labels" . | nindent 4 }}
    {{- include "smartdocs.postgres.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "smartdocs.postgres.selectorLabels" . | nindent 4 }}
